<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- 
    =================================================================
    PANEL STYLES - MODERN DESIGN
    =================================================================
    Borders, Cards, Panels, and Container styles
    Date: October 28, 2025
    =================================================================
    -->
    
    <!-- ============================================================
         CARD PANEL (Elevated panel with shadow)
         ============================================================ -->
    
    <Style x:Key="CardPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
        <Setter Property="Effect" Value="{StaticResource SubtleShadow}"/>
    </Style>
    
    <!-- ============================================================
         ELEVATED PANEL (Slightly raised, no shadow)
         ============================================================ -->
    
    <Style x:Key="ElevatedPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ElevatedSurface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusSmall}"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
    
    <!-- ============================================================
         SECTION PANEL (For grouping related fields)
         ============================================================ -->
    
    <Style x:Key="SectionPanel" TargetType="GroupBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeM}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Border Background="{StaticResource SecondaryBackground}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{StaticResource CornerRadiusMedium}"
                            Effect="{StaticResource SubtleShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Header -->
                            <Border Grid.Row="0"
                                    Background="{StaticResource ElevatedSurface}"
                                    Padding="12,8"
                                    CornerRadius="6,6,0,0">
                                <ContentPresenter ContentSource="Header"
                                                  TextBlock.Foreground="{StaticResource PrimaryText}"
                                                  TextBlock.FontWeight="SemiBold"/>
                            </Border>
                            
                            <!-- Content -->
                            <ContentPresenter Grid.Row="1"
                                              Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ============================================================
         STATUS PANEL (Success, Warning, Error, Info)
         ============================================================ -->
    
    <Style x:Key="StatusPanel" TargetType="Border">
        <Setter Property="BorderThickness" Value="1,1,1,1"/>
        <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusSmall}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
    
    <Style x:Key="SuccessPanel" TargetType="Border" BasedOn="{StaticResource StatusPanel}">
        <Setter Property="Background" Value="#10451D"/>
        <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
    </Style>
    
    <Style x:Key="WarningPanel" TargetType="Border" BasedOn="{StaticResource StatusPanel}">
        <Setter Property="Background" Value="#451A03"/>
        <Setter Property="BorderBrush" Value="{StaticResource WarningColor}"/>
    </Style>
    
    <Style x:Key="ErrorPanel" TargetType="Border" BasedOn="{StaticResource StatusPanel}">
        <Setter Property="Background" Value="#450A0A"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorColor}"/>
    </Style>
    
    <Style x:Key="InfoPanel" TargetType="Border" BasedOn="{StaticResource StatusPanel}">
        <Setter Property="Background" Value="#172554"/>
        <Setter Property="BorderBrush" Value="{StaticResource InfoColor}"/>
    </Style>
    
    <!-- ============================================================
         TOOL SELECTOR PANEL (Top bar with tool dropdown)
         ============================================================ -->
    
    <Style x:Key="ToolSelectorPanel" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource ElevatedSurface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Effect" Value="{StaticResource SubtleShadow}"/>
    </Style>
    
    <!-- ============================================================
         TAB CONTROL STYLE (Modern tabs with accent bars)
         ============================================================ -->
    
    <Style x:Key="ModernTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
    </Style>
    
    <Style x:Key="ModernTabItem" TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
        <Setter Property="FontFamily" Value="{StaticResource PrimaryFont}"/>
        <Setter Property="FontSize" Value="{StaticResource FontSizeS}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="Margin" Value="0,0,2,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="border"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Accent Bar (Top) -->
                            <Border x:Name="accentBar"
                                    Grid.Row="0"
                                    Height="3"
                                    Background="Transparent"
                                    CornerRadius="2,2,0,0"
                                    Margin="0,0,0,4"
                                    Visibility="Collapsed"/>
                            
                            <!-- Content -->
                            <ContentPresenter Grid.Row="1"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                            <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
                            <Setter TargetName="border" Property="Background" Value="{StaticResource ElevatedSurface}"/>
                            <Setter TargetName="accentBar" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="accentBar" Property="Background" Value="{StaticResource PrimaryAccent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ============================================================
         SEPARATOR LINE
         ============================================================ -->
    
    <Style x:Key="ModernSeparator" TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource BorderColor}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="0,8,0,8"/>
    </Style>
    
    <!-- ============================================================
         SCROLLBAR STYLE
         ============================================================ -->
    
    <Style x:Key="ModernScrollBar" TargetType="ScrollBar">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"/>
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border x:Name="thumbBorder"
                                                    Background="{StaticResource BorderColor}"
                                                    CornerRadius="4"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource SecondaryText}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>

