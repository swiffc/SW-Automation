<UserControl x:Class="UnifiedUI.Views.XCHStructurePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource PrimaryBackground}">
    
    <UserControl.Resources>
        <!-- Apply dark theme to ALL controls -->
        <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource ModernComboBox}"/>
        <Style TargetType="Label" BasedOn="{StaticResource ModernLabel}"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PrimaryText}"/>
        </Style>
        <Style TargetType="Border" x:Key="SectionBorder">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="{StaticResource CornerRadiusMedium}"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect" Value="{StaticResource SubtleShadow}"/>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="15">
        <StackPanel MaxWidth="800">
            
            <Border Background="{StaticResource ElevatedSurface}" 
                    BorderBrush="{StaticResource SuccessColor}" 
                    BorderThickness="2" 
                    CornerRadius="{StaticResource CornerRadiusMedium}" 
                    Padding="20" 
                    Margin="0,0,0,15"
                    Effect="{StaticResource SubtleShadow}">
                <StackPanel>
                    <TextBlock Text="XCH STRUCTURE CONFIGURATION" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource SuccessColor}" 
                               Margin="0,0,0,15" 
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Design Table Approach - Excel-Driven" 
                               FontSize="14" 
                               Foreground="{StaticResource SecondaryText}" 
                               HorizontalAlignment="Center" 
                               Margin="0,0,0,10"/>
                    <TextBlock Text="Generate: XCH cooler structure with fan configuration" 
                               FontSize="12" 
                               Foreground="{StaticResource SecondaryText}" 
                               TextWrapping="Wrap" 
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <Expander Header="JOB INFORMATION" 
                      IsExpanded="True" 
                      Margin="0,0,0,15">
                <Expander.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource PrimaryAccent}" 
                                   Margin="5"/>
                    </DataTemplate>
                </Expander.HeaderTemplate>
                <Border Style="{StaticResource SectionBorder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Job Number:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding GlobalJobNumber, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="5" Margin="0,5"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Part Prefix:" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding GlobalPartPrefix, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="5" Margin="0,5"/>
                </Grid>
                </Border>
            </Expander>

            <Expander Header="XCH CONFIGURATION" 
                      IsExpanded="False" 
                      Margin="0,0,0,15">
                <Expander.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Foreground="{StaticResource WarningColor}" 
                                   Margin="5"/>
                    </DataTemplate>
                </Expander.HeaderTemplate>
                <Border Style="{StaticResource SectionBorder}">
                    <StackPanel>
                        <TextBlock Text="(Coming Soon)" 
                                   Foreground="{StaticResource SecondaryText}" 
                                   FontStyle="Italic" 
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="• Fan Ring Dimensions" Margin="0,5" Foreground="{StaticResource SecondaryText}"/>
                        <TextBlock Text="• Fan Count and Layout" Margin="0,5" Foreground="{StaticResource SecondaryText}"/>
                        <TextBlock Text="• Support Structure" Margin="0,5" Foreground="{StaticResource SecondaryText}"/>
                        <TextBlock Text="• Access Platform" Margin="0,5" Foreground="{StaticResource SecondaryText}"/>
                    </StackPanel>
                </Border>
            </Expander>

        </StackPanel>
    </ScrollViewer>
</UserControl>
